import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { LanguageProvider } from "./context/LanguageContext";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ray Deng | Founder · Operator · Cross-border Builder",
  description: "Building ventures across China, New Zealand & Australia. AI-enabled systems, workforce mobility platforms, and cross-border commerce infrastructure.",
  keywords: "Ray Deng, 大瑞, Founder, Entrepreneur, New Zealand, China, Australia, AI, WorkVisas, Cross-border",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <LanguageProvider>
          {children}
        </LanguageProvider>
      </body>
    </html>
  );
}
